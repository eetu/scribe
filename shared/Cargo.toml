[package]
name = "scribe-shared"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
# For the shared shutdown handler (see src/shutdown.rs) — all three binaries
# already depend on both, so this adds no build weight.
tokio = { workspace = true }
tracing = { workspace = true }
